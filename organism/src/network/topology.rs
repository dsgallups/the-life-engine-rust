use crate::CellTemplate;

pub struct NetworkTopology {}

impl NetworkTopology {
    pub fn new_from_cells(cell_templates: Vec<CellTemplate>) -> Self {
        for cell in cell_templates {
            todo!()
        }

        todo!()
    }
}
